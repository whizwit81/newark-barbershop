import { useState } from "react"
import { getAppointments } from "../Services/ScheduledApptService.jsx"

const EditDropDownAppointment = () => {
    const [options, setOptions] = useState([])
    const [selectedOption, setSelectedOption] = useState(``)

const fetchData = () => 
}

